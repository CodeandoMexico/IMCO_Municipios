<div id="form-container">
  <%= form_for  @user, html: { class: 'form-horizontal' } do |f| %>
  <% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@dependencies.errors.count, "error") %> prohibited this lines from being saved:</h2>

    <ul>
      <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="form-group">
    <%= f.label t('dashboard.contacts.edit.correo'), class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :email, class: 'form-control' %>
    </div>
  </div>

   <div class="form-group">
    <%= f.label t('dashboard.contacts.edit.password'), class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :password, class: 'form-control' %>
    </div>
  </div>

  <div class="actions bg-primary-nergro">
    <%= f.submit t('dashboard.contacts.edit.ok'), :class => 'btn btn-success btn-sm outlineverde' %>
  </div>
  <% end %>
</div>
