%nav.navbar.navbar-default.navbar-static-top{ :role => "navigation", :style =>"margin-bottom: 0"}
  %div.navbar-header
    %button{:type=> "button", :class=> "navbar-toggle", :data => { :toggle => "collapse", :target=> ".navbar-collapse"}}
      %span.sr-only
        Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    - if current_user
      = link_to "Mi Negocio #{City.find(current_user.city_id).name}", dashboard_path , :class => 'navbar-brand'